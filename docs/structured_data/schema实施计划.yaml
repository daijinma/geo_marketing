# Schema 实施计划

项目名称: [项目名称]
创建日期: YYYY-MM-DD
负责人: [负责人]
最后更新: YYYY-MM-DD

# Schema 实施清单
实施计划:
  # 首页
  - 页面名称: "首页"
    页面URL: "https://example.com/"
    页面类型: "首页"
    优先级: "高"
    状态: "pending"  # pending / in_progress / completed
    预计完成日期: YYYY-MM-DD
    Schema类型:
      - type: "Organization"
        priority: "高"
        status: "pending"
        template: "templates/organization_schema.yaml"
      - type: "BreadcrumbList"
        priority: "中"
        status: "pending"
        template: "templates/breadcrumb_schema.yaml"
    备注: "[备注信息]"

  # 服务页
  - 页面名称: "服务介绍页"
    页面URL: "https://example.com/services"
    页面类型: "服务页"
    优先级: "高"
    状态: "pending"
    预计完成日期: YYYY-MM-DD
    Schema类型:
      - type: "Service"
        priority: "高"
        status: "pending"
        template: "templates/service_schema.yaml"
      - type: "LocalBusiness"
        priority: "高"
        status: "pending"
        template: "templates/localbusiness_schema.yaml"
      - type: "AggregateRating"
        priority: "中"
        status: "pending"
        template: "templates/aggregaterating_schema.yaml"
    备注: "[备注信息]"

  # FAQ 页
  - 页面名称: "FAQ 页面"
    页面URL: "https://example.com/faq"
    页面类型: "FAQ 页"
    优先级: "高"
    状态: "pending"
    预计完成日期: YYYY-MM-DD
    Schema类型:
      - type: "FAQPage"
        priority: "高"
        status: "pending"
        template: "templates/faq_schema.yaml"
    备注: "[备注信息]"

  # 博客文章
  - 页面名称: "博客文章"
    页面URL: "https://example.com/blog/*"
    页面类型: "博客文章"
    优先级: "中"
    状态: "pending"
    预计完成日期: YYYY-MM-DD
    Schema类型:
      - type: "Article"
        priority: "中"
        status: "pending"
        template: "templates/article_schema.yaml"
      - type: "BreadcrumbList"
        priority: "中"
        status: "pending"
        template: "templates/breadcrumb_schema.yaml"
    备注: "[备注信息]"

# 优先级统计
优先级统计:
  高优先级: 0
  中优先级: 0
  低优先级: 0

# 状态统计
状态统计:
  pending: 0
  in_progress: 0
  completed: 0

