[project]
name = "geo-marketing"
version = "0.1.0"
description = "GEO Marketing - 生成式引擎优化实时监测平台"
readme = "README.md"
requires-python = ">=3.12"

[tool.uv]
dev-dependencies = []

[tool.uv.workspace]
members = [
    "geo_db",
    "geo_server",
]

