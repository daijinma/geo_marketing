[project]
name = "llm-sentry-geo"
version = "0.1.0"
description = "LLM Sentry: GEO (生成式引擎优化) 实时监测平台 - Monorepo"
readme = "README.md"
requires-python = ">=3.12"

[tool.uv]
workspace = true
dev-dependencies = []

[tool.uv.workspace]
members = [
    "geo_db",
    "llm_sentry_monitor",
]

